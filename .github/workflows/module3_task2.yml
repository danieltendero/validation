name: module3_task2
on:
  push:
  schedule:
    # * is a special character in YAML so you have to quote this string
    - cron: '30 5,17 * * *'
  workflow_call:

jobs:
  call-workflow:
    uses: .github/workflows/module3_task1.yml@v1
